<UserControl x:Class="AuroraVoiceAtis.Views.Editors.StringCollectionEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AuroraVoiceAtis.Views.Editors"
             xmlns:vm="clr-namespace:AuroraVoiceAtis.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <vm:StringCollectionEditorViewModel />
    </UserControl.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ComboBox 
            Name="ComboboxItems"
            ItemsSource="{Binding Items}"
            SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
            IsEditable="{Binding IsEditing}"
            Text="{Binding CurrentText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        </ComboBox>
        <Button
            Grid.Column="1"
            Margin="2"
            Padding="5,0,5,0"
            Content="+"
            Command="{Binding AddItemCommand}" />
        <Button
            Grid.Column="2"
            Margin="2"
            Padding="5,0,5,0"
            Content="X"
            Command="{Binding RemoveItemCommand}" />
    </Grid>
</UserControl>
